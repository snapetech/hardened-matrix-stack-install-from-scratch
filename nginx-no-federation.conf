# Block federation at the proxy when federation is disabled. Include in matrix 443 server block BEFORE location /_matrix.
# Prevents any /_matrix/federation or .well-known server discovery even if Synapse config is misconfigured.
location /_matrix/federation {
    return 403;
}
location = /.well-known/matrix/server {
    return 404;
}
