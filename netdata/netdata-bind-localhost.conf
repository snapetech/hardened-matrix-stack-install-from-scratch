# Bind Netdata web UI to localhost only (nginx reverse-proxies /metrics/ with Synapse auth).
# Copy to /etc/netdata/netdata.conf.d/bind-localhost.conf (or merge into netdata.conf [web] section).
[web]
    bind socket to IP = 127.0.0.1
