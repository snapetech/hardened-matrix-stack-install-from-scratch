# Softer sshd jail: avoid banning same-IP during normal use (e.g. a few sudo
# attempts, concurrent SSH). Default is often maxretry=3–5; 10 failures in
# 10m required before ban. Reload: sudo fail2ban-client reload

[sshd]
enabled = true
maxretry = 10
findtime = 10m
bantime = 10m
