# Snippets for MAS (Matrix Authentication Service) at https://matrix.example.com/auth/
# Insert into the 443 server block for your Matrix host, BEFORE location /_matrix and location / .
#
# 1) MAS at /auth/
#    location /auth/ {
#        proxy_pass http://127.0.0.1:8080/;
#        proxy_http_version 1.1;
#        proxy_set_header Host $host;
#        proxy_set_header X-Real-IP $remote_addr;
#        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#        proxy_set_header X-Forwarded-Proto $scheme;
#    }
#
# 2) Compatibility layer: send login/logout/refresh to MAS
#    location ~ ^/_matrix/client/(.*)/(login|logout|refresh)$ {
#        proxy_http_version 1.1;
#        proxy_pass http://127.0.0.1:8080;
#        proxy_set_header Host $host;
#        proxy_set_header X-Real-IP $remote_addr;
#        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#        proxy_set_header X-Forwarded-Proto $scheme;
#    }
